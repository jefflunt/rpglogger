%span{:class=>'color-orange'}='SECTIONS '
%span=link_to '(edit)', edit_log_book_path(@log_book)
%table{:class=>'max-width'}
  %tr
    %td
      %ul{:class=>'list-no-markers'}
        -@log_book.sections.each_with_index do |s, index|
          %li=link_to '>&nbsp;'.html_safe+s.name, "/log_books/#{@log_book.id}?section=#{s.name}"