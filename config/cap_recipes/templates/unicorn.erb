working_directory deploy_to

pid "#{shared_path}/pids/unicorn.rpglogger.pid"
stderr_path "#{current_path}/log/unicorn.rpglogger.log"
stdout_path "#{current_path}/log/unicorn.rpglogger.log"

listen "/tmp/unicorn.rpglogger.sock"
worker_processes 2
timeout 30
