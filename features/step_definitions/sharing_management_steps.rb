When /^I try to change the access list of LogBook "(.*?)" to add "(.*?)" with "(.*?)" access$/ do |log_book_title, user_nickname, access_level|
  pending
  # log_book = LogBook.find_by_title(log_book_title)
  # user = User.find_by_nickname(user_nickname)
  # 
  # step "I go to the edit LogBook page for \"#{log_book_title}\""
  # step "I fill in \"new_#{user.provider}_user\" with \"#{user_nickname}\""
  # step "I press \"Add users\""
end